syntax = "proto3";
package jpyx.pricefeed;

import "gogoproto/gogo.proto";
// this line is used by starport scaffolding # genesis/proto/import
import "pricefeed/pricefeed.proto";

option go_package = "github.com/lcnem/jpyx/x/pricefeed/types";

// GenesisState defines the pricefeed module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.moretags) = "yaml:\"params\""];
  repeated PostedPrice posted_prices = 2 [(gogoproto.moretags) = "yaml:\"posted_prices\""];
  // this line is used by starport scaffolding # genesis/proto/state
}
